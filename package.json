{
  "name": "express-subdomain-router",
  "version": "1.0.0",
  "description": "Express.js server handling dynamic custom subdomains natively for Render Free Plan deployment.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
